import { SearchTestimonialnamePipe } from './search-testimonialname.pipe';

describe('SearchTestimonialnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTestimonialnamePipe();
    expect(pipe).toBeTruthy();
  });
});
